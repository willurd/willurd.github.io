{
  "name": "my-resume",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "classnames": "^2.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-photo-album": "^2.4.1",
    "react-scripts": "1.1.4"
  },
  "devDependencies": {
    "cross-env": "^5.1.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "cross-env PUBLIC_URL=/resume react-scripts build && rm -rf ../resume && mv build ../resume",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
